import { useEffect, useMemo, useState } from "react";
import { supabase } from "./lib/supabase";
import { getCardFunnel, type CardEvent } from "./cardFunnel";
import { getStaffPerformance } from "./cardStaffPerformance";
import { getSalesInsights } from "./cardSalesInsights";
import CardMonthlyPerformance from "./CardMonthlyPerformance";
import CardLongTermPerformance from "./CardLongTermPerformance";
import CardActionCenter from "./CardActionCenter";
import CardAbPerformance from "./CardAbPerformance";
import CardSharePerformance from "./CardSharePerformance";
type Period=7|30|0; type LeadRow={status?:string|null;source_card?:string|null;source_staff?:string|null;created_at?:string|null};
const PERIOD_LABEL:Record<Period,string>={7:"최근 7일",30:"최근 30일",0:"전체"}; const LEVEL_LABEL={good:"잘하고 있는 점",watch:"확인할 점",action:"우선 개선"} as const;
export default function CardFunnelPanel(){const[period,setPeriod]=useState<Period>(30);const[events,setEvents]=useState<CardEvent[]>([]);const[leads,setLeads]=useState<LeadRow[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);useEffect(()=>{void load()},[period]);async function load(){if(!supabase)return;setLoading(true);setError(null);const since=period?new Date(Date.now()-period*86400000).toISOString():null;let eq=supabase.from("miso_card_events").select("event_type,card_id,staff_name,created_at").order("created_at",{ascending:false}).limit(5000);let lq=supabase.from("partner_applications").select("status,source_card,source_staff,created_at").or("source_card.not.is.null,source_staff.not.is.null");if(since){eq=eq.gte("created_at",since);lq=lq.gte("created_at",since)}const[e,l]=await Promise.all([eq,lq]);if(e.error||l.error)setError(e.error?.message||l.error?.message||"통계를 불러오지 못했습니다.");setEvents((e.data??[]) as CardEvent[]);setLeads((l.data??[]) as LeadRow[]);setLoading(false)}const stats=useMemo(()=>getCardFunnel(events,leads.filter(x=>x.status==="approved").length),[events,leads]);const staff=useMemo(()=>getStaffPerformance(events,leads),[events,leads]);const applications=leads.length;const appRate=stats.views?Math.round(applications/stats.views*100):0;const topStaff=staff.find(x=>x.approved>0)??null;const insights=useMemo(()=>getSalesInsights({views:stats.views,contacts:stats.contacts,contactRate:stats.contactRate,leadClicks:stats.leadClicks,applications,approved:stats.approvedLeads,approvalRate:stats.approvalRate,saves:stats.saves,shares:stats.shares,topStaff}),[stats,applications,topStaff]);const steps=[["명함 조회",stats.views,"100%"],["전화·카톡·문자",stats.contacts,`${stats.contactRate}%`],["신규거래 클릭",stats.leadClicks,`${stats.leadClickRate}%`],["신규거래 신청",applications,`${appRate}%`],["거래 승인",stats.approvedLeads,`${stats.approvalRate}%`]] as const;return <><section className="admin-panel"><div className="admin-panel-title"><div><span>MISO CARD FUNNEL</span><h2>전자명함 영업 퍼널</h2><small>명함을 본 사람이 실제 거래로 이어지는 과정을 확인합니다.</small></div><div style={{display:"flex",gap:6,flexWrap:"wrap"}}>{([7,30,0] as Period[]).map(p=><button key={p} className={period===p?"active":""} onClick={()=>setPeriod(p)}>{PERIOD_LABEL[p]}</button>)}</div></div>{error&&<div className="admin-notice error">{error}</div>}{loading?<p className="admin-empty">MISO CARD 통계를 불러오는 중입니다.</p>:<><div className="admin-stats"><article><div><b>{stats.views}</b><span>명함 조회</span></div></article><article><div><b>{stats.contacts}</b><span>상담 클릭 · {stats.contactRate}%</span></div></article><article><div><b>{applications}</b><span>신규거래 신청 · {appRate}%</span></div></article><article><div><b>{stats.approvedLeads}</b><span>거래 승인 · {stats.approvalRate}%</span></div></article></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>단계</th><th>건수</th><th>조회 대비</th></tr></thead><tbody>{steps.map(([a,b,c])=><tr key={a}><td><strong>{a}</strong></td><td>{b}건</td><td>{c}</td></tr>)}</tbody></table></div><div style={{display:"flex",gap:8,flexWrap:"wrap",marginTop:12}}><small>연락처 저장 {stats.saves}건</small><small>공유·링크복사·QR {stats.shares}건</small><small>기간: {PERIOD_LABEL[period]}</small></div>{insights.length>0&&<section style={{marginTop:24}}><h3>대표님께 드리는 자동 영업 제안</h3><div style={{display:"grid",gap:10}}>{insights.map((x,i)=><article key={i} style={{border:"1px solid #dfe7ed",borderRadius:14,padding:16}}><small>{LEVEL_LABEL[x.level]}</small><strong style={{display:"block",marginTop:5}}>{x.title}</strong><p>{x.message}</p><b>추천 실행: {x.action}</b></article>)}</div></section>}{staff.length>0&&<><h3 style={{marginTop:24}}>직원·대표별 영업성과</h3><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>순위</th><th>담당자</th><th>조회</th><th>상담</th><th>상담률</th><th>신청</th><th>신청률</th><th>승인</th><th>승인율</th></tr></thead><tbody>{staff.map((x,i)=><tr key={x.name}><td>{i+1}</td><td><strong>{x.name}</strong></td><td>{x.views}</td><td>{x.contacts}</td><td>{x.contactRate}%</td><td>{x.applications}</td><td>{x.applicationRate}%</td><td>{x.approved}</td><td>{x.approvalRate}%</td></tr>)}</tbody></table></div></>}{stats.views===0&&<p className="admin-empty">아직 실제 사용 데이터가 없습니다.</p>}<CardActionCenter events={events} leads={leads}/></>}</section><CardAbPerformance/><CardSharePerformance/><CardMonthlyPerformance/><CardLongTermPerformance/></>}
